# decisions.md — plainbrain

Append-only log of what changed and why. Newest entry is always the last line. This is the
kit dogfooding its own convention: the distribution ships its own audit trail. The *what* is in
git; this file holds the *why*.

2026-07-02 16:10: adopted the kit repo into its own layout (decisions.md + .claude/plainbrain marker; .gitignore narrowed from all of .claude/ to just .claude/plainbrain + .claude/state/) because the flagship of "every project carries decisions.md" was developing with commit messages as its only why-trail — during a Fable architecture audit the rationale for contested calls was unrecoverable, the exact archaeology the system exists to prevent (audit finding P3).
2026-07-02 16:10: ran a Fable-tier (xhigh) whole-architecture audit focused on data gathering, data retrieval, and minimalism; it found the system healthy (~1,630 lines, thesis intact) with four structural problems — an undocumented second schema, safety-net promises the code doesn't keep, wiki-surface.sh drifting into "smart substrate," and retrieval recall hanging on one-line index summaries. Acting on it as a phased refactor.
2026-07-02 16:10: on the audit's schema split (G1), chose to DOCUMENT the trust-roster/attributed-map/`by:`+`depth:` model into wiki/CLAUDE.md and scaffold entities/people.md, rather than strip it from the skills, because the local (richer) system is meant to converge with the kit — not diverge from it — and multi-agent portability wants the page schema explicit so any agent following the instruction files produces conforming pages.
2026-07-02 16:20: session-end/pre-compact now snapshot the FULL working tree — untracked files too, via a grafted third stash parent (new shared global/hooks/wip-lib.sh) — because git stash create captures only tracked changes while the README promises "forgetting is always recoverable"; kept the stash-format commit so the plainbrain CLI's recover/show and its merge-on-recover stay unchanged (audit G3).
2026-07-02 16:20: session-end now snapshots + flags on ANY dirty exit, dropping the "distill didn't run this session" guard and its read of the shared .last-distill, because that guard skipped post-distill leftovers the distill skill promises to catch and let concurrent sessions suppress each other's snapshot; a clean tree already no-ops (audit G2). check.sh now scopes to the earliest per-session start marker since the last distill rather than the shared marker, so a concurrent session can't fast-path a distill past another session's commits (audit G5).
2026-07-02 16:26: retrieval — converged the three recall keys (index summary, frontmatter tags, filename) by teaching wiki-query to also tag-grep and list filenames, and wiki-ingest to reuse existing tags before coining new ones (audit R1/R2/G7). Deliberately did NOT also copy tags into index.md (the audit offered "and/or"): the index stays a terse map and tags keep a single home in frontmatter — duplicating them invites drift between two places.
2026-07-02 16:26: the .claude/plainbrain marker now carries an optional `driver: <file>` line that adopt-project writes and session-start reads, replacing the file-existence guess (plan.md wins) that mis-detected CONTEXT-driven hybrids also carrying a plan.md (audit G8). Also: session-start now surfaces a non-empty decisions.scratch left by a dead session (G4); wiki-check emits a tag histogram flagging unsurfaceable tags and lists notes touched since the last lint as promotion candidates (R4/G7/G6).
2026-07-02 16:31: enacted the schema decision — documented the attribution model (roster/tiers, source `by:`+`depth:`, the `## The map` attributed-claim format, the human-only `## My read` zone, `comparisons/` disputes) in wiki/CLAUDE.md, and scaffolded entities/people.md so a fresh install stops dangling on wiki-query's roster reference (audit G1). people.md is a normal indexed+linked entity page (linked from overview.md and index.md) rather than a linter special-case, so wiki-check stays dumb; install.sh now ships it (install only copies top-level wiki/*.md).
2026-07-02 16:35: wiki-surface — cut the summarizer (the hook now injects title + path + matched tag and lets the model open the page itself), externalized the tunable stoplist to ~/.config/plainbrain/stoplist (+ PLAINBRAIN_WIKI_SURFACE_MINLEN), and prune dedupe files older than 7 days (audit P1/P2/R6). The line count barely moved (~218→217): the summarizer was only ~20 lines and the P2/R6 fixes added back what it removed. The win is architectural, not size — the "smart substrate" that pre-decided what 500 chars the model saw is gone, the untrusted-source-body → context injection seam is closed (only Claude-written frontmatter title is injected), and stoplist tuning now survives update.sh. Kept the DF-rarity ranking, type-priority, cap-2, and session-dedupe (the audit said shrink, not cut).
2026-07-02 16:40: dropped wiki/log.md — the git commit history is the same activity record with better fidelity, and every ingest/query/lint already commits, so the log file was a redundant, drift-prone maintenance step in all three wiki skills (audit minimalization; "git is the proof"). Removed it from the schema, the three skills, wiki-check's dead-link loop, and USAGE; repointed wiki-lint's staleness pass to git dates. Also dropped the `updated:` frontmatter field (G10): nothing bumped it, so it was a standing lie — git already has last-modified. Kept `created:`.
2026-07-02 16:40: kept overview.md rather than dropping/demoting it (diverges from the audit's minimalization suggestion) because it's the inbound anchor for the new entities/people.md and a legitimate sparse human entry point the schema already scopes ("a short map, not a dump"); its "no maintainer" critique is mild for a deliberately human-curated page.
2026-07-02 16:40: deferred two Phase-4 minimalization items to review rather than doing them unattended — folding USAGE.md into README (a ~100-line rewrite of published prose, voice-sensitive) and compressing distill/SKILL.md (restructuring a load-bearing skill). Both are judgment calls better made with eyes on; the structural cuts (log.md, updated:) were done.
2026-07-02 16:54: enacted the two deferred Phase-4 items. Folded USAGE.md into README (moved its unique high-value sections — the "where does this go" routing table, rules of thumb, and anti-patterns — verbatim into README, dropped the loops/cadence/troubleshooting that duplicated README, deleted USAGE); nothing linked to USAGE so no references broke. Kept the public-repo voice per entities/me.md — plain first-principles, no vs-X framing (the surfaced plainbrain-vs-* comparison pages stay internal). On distill: did a LIGHT compression (step 2 is now detection-only, its one routing detail — skill location — moved into step 3) rather than the audit's "one table" merge, because steps 2/3 are detect-vs-route+format, not true duplication, and cramming the format rules into a table would harm a load-bearing skill for ~10 lines. Net this turn: -103 lines; branch is now ~+32 over main.
2026-07-02 17:53: verified against opencode's own docs (opencode.ai/docs — rules, config, skills) that the memory/rules/skills layer ports to opencode with zero code — opencode reads the Claude-compat paths natively: `~/.claude/CLAUDE.md` for the global rules and a project's `CLAUDE.md` when it has no `AGENTS.md`, and `~/.claude/skills/<name>/SKILL.md` for the five skills (opencode selects a skill by matching the request to its description via a built-in `skill` tool, not a slash command). So everything install.sh already lands under `~/.claude` is exactly where opencode looks, and install.sh needs no non-hook change.
2026-07-02 17:53: decided NOT to add `AGENTS.md` files for opencode (the obvious-looking port), because opencode reads `AGENTS.md` *instead of* `CLAUDE.md` when both exist — an AGENTS.md would shadow the CLAUDE.md driver-pointer and split the rules across two files to keep in sync, the exact duplication/drift the kit exists to kill. Single source (CLAUDE.md), read by both harnesses. Consequence: the only harness-specific surface left is the four lifecycle hooks (Claude fires them from settings.json, which opencode ignores in favor of plugins on its own event bus) — ported separately. Also neutralized the two Claude-only phrases in distill's description ("Claude Code work session", "before /exit") so opencode's description-based skill selection reads it right, and rewrote the README "Not just Claude" section to this verified reality.
2026-07-02 19:25: REVERSED the 17:53 "do NOT add AGENTS.md" call — supersedes 2026-07-02 17:53. It rested on a wrong fact (that Claude reads AGENTS.md as a fallback). Claude Code's official docs are explicit — "Claude Code reads CLAUDE.md, not AGENTS.md" (code.claude.com/docs/en/memory) — no fallback. So AGENTS.md is now the canonical per-project rules file (opencode — the primary driver going forward — reads it directly), and each project also carries a one-line CLAUDE.md that imports it (`@AGENTS.md`) so secondary Claude Code still loads the rules: the pattern Anthropic's own docs recommend over a symlink, and Windows-safe. One source (AGENTS.md), no duplication, NO symlink (explicit user call). Global rules stay at ~/.claude/CLAUDE.md — the one path both agents read (Claude native, opencode fallback); renaming it would split it across dirs for nothing. Changed: project-template (git mv CLAUDE.md→AGENTS.md + a one-line CLAUDE.md `@AGENTS.md` stub), adopt-project, distill, project-template/ARCHITECTURE.md, global/CLAUDE.md wording, and README (incl. reframing the "wired for Claude Code" opening now that opencode is primary). Existing adopted projects keep their CLAUDE.md and still work on both — migration optional. Deferred and flagged, not changed: the wiki's own ~/wiki/CLAUDE.md schema file and the skills' "read its CLAUDE.md" references (separate repo, ripples through four skills).
2026-07-02 19:42: gave plainbrain its own root AGENTS.md. The kit prescribes "every project carries AGENTS.md," but the kit repo itself was adopted core-only (decisions.md + marker, no instruction file), so it wasn't dogfooding its own convention and opencode found no project rules on opening the repo — the gap I'd only half-closed by adding AGENTS.md to project-template/ (the copy for users) while leaving the repo root bare. Core-only, so it points at README (the map) and decisions.md (the why) with no plan.md/CONTEXT.md driver. No CLAUDE.md stub here — this is an opencode-primary repo; add a one-line `@AGENTS.md` CLAUDE.md only if it's ever developed in Claude Code.
2026-07-02 20:14: went fully opencode-only for the shipped kit — supersedes the 19:25 "AGENTS.md + CLAUDE.md stub" approach. There is now NO CLAUDE.md anywhere in the repo: renamed global/CLAUDE.md→global/AGENTS.md and wiki/CLAUDE.md→wiki/AGENTS.md (the extensive content moves into AGENTS.md, it's not a thin stub), deleted the project-template CLAUDE.md stub, and did not add a root one. install.sh now lands the global rules at ~/.config/opencode/AGENTS.md (opencode's native global path) rather than ~/.claude/CLAUDE.md. Rationale (Chris): Claude reads only CLAUDE.md and opencode only AGENTS.md, so a shipped stub was the only way to serve both from the kit — but opencode is the driver now, so ship AGENTS.md-only and let the rare Claude user rename the file or drop in a one-line @AGENTS.md CLAUDE.md themselves (documented in the README's "Using Claude Code?" note). Updated adopt-project (no more stub creation), the three wiki skills + distill (the wiki layout doc is AGENTS.md now), project-template, and README. Skills/hooks/CLI still install under ~/.claude (opencode reads ~/.claude/skills natively; the hooks are the separate port). Flagged: Chris's live ~/wiki/CLAUDE.md is a separate repo — rename it to AGENTS.md next.

2026-07-03 16:08: added Pi (pi.dev, Mario Zechner / earendil-works; v0.80.3 local) as a first-class harness target — the first shipped non-Claude lifecycle adapter, closing the "another agent re-homes the four scripts" promise the README had made but never delivered (there was never an opencode plugin either). Data + per-project AGENTS.md already worked with Pi natively; added the three missing pieces without touching the four hook scripts: (1) new global/pi/extensions/plainbrain/index.ts — a thin TypeScript extension mapping Pi's lifecycle events to the same ~/.claude/hooks/*.sh (one script copy serves both harnesses); (2) install.sh gained a Pi target (auto-detected via `command -v pi`/`~/.pi`, `--pi`/`--no-pi` override) that drops global rules at ~/.pi/agent/AGENTS.md, symlinks each skill into ~/.pi/agent/skills/, and installs the extension — the kit-owned bits live in install_kit() so `--update` refreshes them; (3) README "Not just Claude" reframed to honest tiers (Claude Code full via settings.json, Pi full via the extension, opencode rules+skills only — no lifecycle adapter yet). The event mapping and its mechanics were reviewed on the Fable model against Pi's actual source before writing, which corrected four things over the naive plan: session-start injects via `pi.sendMessage(..., {deliverAs:"nextTurn"})` from `session_start` (that event can't return context) rather than a once-flag on first `before_agent_start` (module state resets on `/reload`); both `session_start` and `session_shutdown` skip the `reload` reason (else session-end.sh plants a phantom .pending-distill on a continuing session); the payload goes to the scripts via a Node `execFile` child's stdin (Pi's `pi.exec` takes only {signal,timeout} — no cwd/env/stdin — and stdin keeps the prompt text out of `ps`); and every handler is wrapped in try/catch + a timeout because a throw from `before_agent_start` breaks the user's turn. Injected messages use `display:false` (in LLM context, not rendered — matches Claude Code's hidden hook-context injection, no custom renderer needed). Skills symlinked per-skill, not the skills root (a root symlink trips a Pi config display bug, #3405). Also fixed global/AGENTS.md's title, which hardcoded the opencode home now that the file lands in multiple homes. Deferred: the wiki page concepts/coding-agent-harnesses.md still describes Pi as "4 tools minimal" — now stale (0.80.3 has skills + 25 lifecycle hooks); fix in a distill pass, not here.
