@AGENTS.md

<!-- This project's rules live in AGENTS.md — the cross-tool standard opencode and others read
     directly. Claude Code reads only CLAUDE.md, so this one-line file imports AGENTS.md. It is a
     plain import, not a symlink: one source of rules, nothing to keep in sync. Edit AGENTS.md, not
     this. Delete this file if you never use Claude Code. -->
